# Graphify output — per-project knowledge graph, built fresh by the skill
graphify-out/

# Local artifact from `graphify install --platform opencode`
# setup.sh handles global config; these are machine-specific
.opencode/
dev-docs/

# Python
__pycache__/
*.pyc

# macOS
.DS_Store

# Graphify cost tracking (per-user, not project data)
cost.json

# Session history and local tooling state
.dev-agent/

# This repo's own AGENTS.md + sidecar are generated by setup.sh with
# machine-specific resolved paths; config/AGENT-STANDING-RULES.md is the
# committed source of truth.
/AGENTS.md
/.agents-md.sha256
