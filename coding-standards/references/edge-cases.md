# Edge Cases — coding-standards (dispatcher)

Accumulated real-world edge cases encountered while using this skill's
dispatch logic specifically (domain detection, task-to-domain matching,
ambiguous dispatch decisions). Consult this file when something unexpected
happens during use; append new entries per the self-improvement protocol
(dated, never rewriting or removing existing entries).

<!-- No entries yet. -->
