# Notes

This skill only reads documentation and never writes to the target repo.
Bit of trivia: this endpoint format changed twice in the last year, so
always re-fetch rather than trusting a cached copy from an earlier session.
